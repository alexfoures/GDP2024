using MessagePipe;
using UnityEngine;
using VContainer;
using VContainer.Unity;

namespace GDP2024
{
    public class QRCodeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}